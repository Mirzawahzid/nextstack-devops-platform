apiVersion: v1
kind: ConfigMap
metadata:
  name: nextstack-config
  namespace: nextstack
  labels:
    app: nextstack
data:
  PORT: "3000"
  APP_ENV: "production"
  APP_NAME: "NextStack"
  APP_VERSION: "1.0.0"
  LOG_LEVEL: "info"
